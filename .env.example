# 复制为 .env.local 后填写。.env.local 已被 .gitignore 排除,永远不要提交真实值。
# 所有包与工具只从环境变量 / 运行时输入读取凭据,代码里不写死。

# 目标 ThingsBoard(生产镜像 LXC 111)
TB_BASE=http://192.168.20.61:8080
TB_USER=tenant@thingsboard.org
TB_PASSWORD=<password>

# Customer 视角预览 / 验收用的 CUSTOMER_USER 账号(T0.4 锁定)
TB_CUSTOMER_USER=<customer-user-email>
TB_CUSTOMER_PASSWORD=<password>

# kzserver 扩展服务(仅 ADR-004 选 ext 路线时需要)
KZ_BASE=http://192.168.20.61:8099

# 大屏第二只读身份(报表账号,CUSTOMER_USER)。仅 apps/deploy-tool 的 site.html 三级取数使用;T3.8 删除后移除
VITE_REPORT_USER=<customer-user-email>
VITE_REPORT_PASSWORD=<password>

# packages/renderer 的 /dev 页「镜像真数据」预填(可选;建议填 CUSTOMER_USER)
VITE_TB_USER=<customer-user-email>
VITE_TB_PASSWORD=<password>
# /dev 页「身份」切换用的租户预填(可选)
VITE_TB_TENANT_USER=tenant@thingsboard.org
VITE_TB_TENANT_PASSWORD=<password>
