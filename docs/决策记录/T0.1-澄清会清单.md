# T0.1 澄清会清单(需核实 / 澄清的问题)

> 目的:同事回填(2026-09-04)后仍卡住契约冻结与 ADR-001 / 003 / 004 的问题,一次会开完。预计 **30 分钟**,建议三人到场(YY、后端 / 扩展服务同事、庄艳芹)。
> **范围说明(YY,2026-09-04)**:D 部分(接线图、宿主接入方式、代码组织、时间投入)**暂缓,本次不谈**,相关条目已移至 §5;本次只谈告警、命名、kz 接口与 tbClient 的 C 部分。
> 用法:每条写清「问什么、为什么要问、希望拿到什么、不问会怎样(默认)」。会上按编号过,结论直接填在「结论」栏;拿不到的东西写明谁、什么时候给。
> 会前请同事准备的材料在 §0,**有材料在手,大部分问题 5 分钟能过**。

---

## 0. 请同事会前带来的材料

| 材料 | 谁 | 用途 |
|---|---|---|
| ① 一份现有「告警模板 JSON」样例(存在 TB 资产上的那种),以及处理它的代码 / 服务入口名 | 后端 | 定 A1:告警到底谁判定、落哪 |
| ② 你们业务代码里对遥测 key 做**前缀 / 模糊匹配**的规则清单(正则或前缀表,如 `ts*`、`hist*`、`REALTIME_*`) | 后端 | 定 A3:我们新 key 的前缀不能撞 |
| ③ kz **通用查询接口**的接口文档或一份真实请求 / 响应样例(路径、参数、返回体、鉴权) | 后端 | 定 A4:`ext` 绑定的 `params` 形状 |
| ④ 你们应用的 `core/request.js`、`core/websocket.js`(看一眼即可) | 庄 | 定 C 部分的包结构 |

---

## 1. 告警(ADR-001)—— 后端同事

| # | 问题 | 为什么要问 | 希望拿到 | 不问的默认 |
|---|---|---|---|---|
| 1.1 | 回填写「通过在资产配置告警模版 json,通过外部工具将 json 处理好,保存到 TB 对应资产」——**谁读这个 JSON 并判定告警?** 是 kz 定时扫遥测,还是 TB 规则链里有节点读它? | 决定告警是「TB 内」还是「TB 外」产生 | 判定方 + 判定周期(实时 / 每分钟 / 每 5 分钟) | 按「kz 判定」理解 |
| 1.2 | 判定后告警**写到哪里**?TB 的 Alarm 表(能在 TB 界面「告警」页看到),还是 kz 自己的表 / 只发微信? | 渲染器的 `mode: 'alarm'` 只能订 TB Alarm 表;落 kz 表就得走 `ext` | 明确答案;若落 TB,是用 TB 的 Create Alarm API 还是规则链节点 | 按「落 TB Alarm 表」理解 |
| 1.3 | 这份 JSON 的**格式**:一条规则长什么样(设备、key、条件、阈值、级别、恢复条件)?支持「变化才报」吗?支持跨设备条件吗? | 我们的编译器要能生成它 | 样例文件(材料 ①) | 编译器先不支持,告警仍走我们现有的站点规则链 |
| 1.4 | 「设备上报故障 – 状态:直接在设备实例上配置」是什么意思?TB 里告警规则只能配在 Profile 上,设备实例上没有——是指 JSON 里按设备写,还是设备属性里存了阈值? | 避免理解错 | 一句话解释 | 按「JSON 里按设备写」理解 |
| 1.5 | 你们的 Device Profile 是**多个项目共用**的吗?(镜像里 IED / METER / ATS 三个 Profile 都无告警规则,129 台仙人山设备在 `default`) | 决定 Profile 路线还有没有存在必要 | 是 / 否 | 按「共用」处理:Profile 只用于通讯中断 inactivity |
| 1.6 | 「通讯中断」你们现在怎么判?仙人山设备的 `COM` 是累加计数不是 0/1 | 决定通讯中断编译成 TB inactivity 事件还是「N 分钟 COM 不变」 | 现行判定规则 + 时长阈值 | 用 TB Profile 的 inactivity(默认 5 分钟) |

**会后要写的 ADR-001 结论形状**:主路线 = ______(资产 JSON / 规则链 / Profile);通讯中断 = ______;告警落点 = ______;渲染器 `mode: 'alarm'` 是否可用 = ______。

## 2. 计算结果命名(ADR-003)—— 后端同事

| # | 问题 | 为什么要问 | 希望拿到 | 不问的默认 |
|---|---|---|---|---|
| 2.1 | 你们的前缀 / 模糊匹配规则具体是什么?(材料 ②) | 我们新生成的 key 要选一个**绝不会被你们匹配到**的前缀 | 规则清单 | 用 `calc_`,并在编译器里对 `calc_*` 加保护:不进你们的归档 |
| 2.2 | 如果我们的工具往 TB 写 `calc_total_p` 这类 key,你们的归档服务会不会**误采**它进归档库? | 防止我们的实时汇聚被你们当成业务量归档 | 会 / 不会;若会,怎么排除(前缀白名单 / 黑名单在哪配) | 假设不会;里程碑 A 后用一个 `calc_` key 实测 |
| 2.3 | 「日电量」「日收益」这类 ≥ 1 天粒度的统计,**以谁为准**?我们建议:一期页面上凡是 ≥ 1 天的统计一律取 kz(`ext`),TB 内只做实时级汇聚(总功率、需量) | 避免同一站点两套口径 | 同意 / 不同意 | 按建议执行 |
| 2.4 | 除了「分 / 5 分 / 时 / 日 / 月 / 年归档」,还有没有「算出来的结果再触发别的计算」的链路在 TB 规则链里(不是 kz 里)? | 决定我们的级联白名单要不要照顾你们的链路 | 有 / 没有 | 按「没有」处理:白名单只管我们自己的链 |

**会后要写的 ADR-003 结论形状**:新输出前缀 = ______;旧输出不改名(已定);≥ 1 天统计取 kz(是 / 否);白名单范围 = 仅自家链路。

## 3. kz 通用接口与 `ext` 绑定(ADR-004)—— 后端同事

| # | 问题 | 为什么要问 | 希望拿到 | 不问的默认 |
|---|---|---|---|---|
| 3.1 | 通用查询接口的**路径与请求体**:按什么查——设备 id?站点 / 业务 id(`businessIds.json` 那种)?指标名?时间段?粒度? | 决定契约里 `ext.params` 的字段 | 材料 ③ | `params` 先按 `{ stationId, metric }` 预留,实装时再改 |
| 3.2 | **返回体**形状:`[{ time, value }]` 数组?按指标分组的对象?字段名是什么? | tbClient 要把它归一成 `{ series: { 名: [{ts, value}] } }` | 一份真实响应 | tbClient 里写适配器,形状变了只改适配器 |
| 3.3 | **鉴权**:通用接口是否与 `stationRevenueTrend` 一样,任意有效 TB token 即可? | 决定 tbClient 是否要为 kz 单独登录 | 是 / 否 | 按「同 TB token」处理 |
| 3.4 | 粒度 `1m / 5m / 1h / 1d / 1M / 1y` 的**参数取值**怎么写?时间段用时间戳还是日期字符串?时区? | 契约 `interval` 枚举要与接口一致 | 参数说明 | 按 kz 现有 `queryType` 风格猜测,实装时改 |
| 3.5 | TB 遥测**保留期**准确是多少天?生产与镜像一致吗?是 TB TTL 还是你们的清理任务? | 决定工具里「≤ N 天走 TB、否则走 kz」的 N | 天数 + 机制 | N = 3 |
| 3.6 | kz 地址在生产 / 镜像 / 现场各是什么形态(同机反代路径 `/kzserver`?独立端口?)?宿主应用现在怎么配它? | tbClient 的 `extBase` 配置项与部署文档 | 各环境地址形态 | 按现状:同 TB 主机 `:8099`,反代路径 `/kzserver` |

**会后要写的 ADR-004 结论**:路线 A 正式采纳;`ext.params` = ______;保留期 N = ______;鉴权 = ______。

## 4. tbClient(C 部分)—— 庄艳芹

| # | 问题 | 为什么要问 | 希望拿到 | 不问的默认 |
|---|---|---|---|---|
| 4.1 | **C4 澄清**:我们要的只是两件事——① TB 推的 `"78.5"` 转成数字 `78.5`;② 查最新值时不存在的 key 给 `null` 不报错。推送是增量的(只推变化的 key)我们知道,契约就是按增量设计的。这样理解没问题吧? | 回填写「无法做到」,疑为理解偏差 | 确认 | 按契约执行 |
| 4.2 | **C7「待沟通」**:`provideTbClient / useTbClient / useTelemetry` 这层 Vue 适配,你的顾虑是什么(与现有 store 冲突?不想维护?)。我们可以由 YY 来写这层,你只提供 `TbClient` 类 | 解开阻塞 | 顾虑 + 分工 | Vue 适配层由 YY 写 |
| 4.3 | **C8 用 JS**:同意 tb-client 用 JS 写;`.d.ts` 由 YY 按 `DataSource` 接口手写并随包发布。你这边只要保证方法名和参数顺序按契约来。OK? | 定下来 | 确认 | 按此执行 |
| 4.4 | **「直接 import」**:包不能反向 import 应用代码。等价做法是把 `core/request.js`、`core/websocket.js` **移进** `packages/tb-client/src/`,你的应用改一行 import 路径指向包(仍然只有一份代码)。能接受吗?(应用怎么接 monorepo 暂不讨论) | 回填意愿是「不要两份代码」 | 确认 | 由 YY 复制两个文件进包,先保里程碑 A,后续再合 |
| 4.9 | **C3 徽标**:我们已改为默认不显示,由宿主决定;但 `status` 仍会通过事件抛出。OK? | 回填意见已采纳,确认即可 | 确认 | 按此执行 |
| 4.10 | 你们应用里现在**登录后 token 存在哪、怎么刷新**?tbClient 的 `setToken()` 要接上它 | C5 落地细节 | 一句话 | 实装时看代码 |

## 5. 暂缓项(YY 指示 2026-09-04,本次不谈)

以下与 D 部分 / 接线图相关,待里程碑 A 之后再议;渲染器 `main` 槽位一期先用 `image` 占位,宿主接入方式按方案二(不动同事仓库)默认推进。

| # | 问题 | 为什么要问 | 希望拿到 | 不问的默认 |
|---|---|---|---|---|
| 4.5 | **D3 代码组织**:你的应用搬进 `dev/apps/host-app`(方案一)还是留原仓库用 `file:` 链接(方案二)? | 影响每天联调节奏 | 选择 | 方案二(不动你的仓库) |
| 4.6 | **D2 误解澄清**:新页面**不需要** node 服务、不需要编译打包——页面配置是运行时从 `ScadaPage` 资产读 JSON 交给 `<ScadaPage>` 渲染,工程人员发布后刷新即见。当场演示:改 JSON → 刷新。 | 回填里担心「可行性需考虑」 | 消除顾虑 | 里程碑 A 演示 |
| 4.7 | **D1 接线图**:源码在哪、依赖了什么?改成只吃 props 预计几天?props 形状 `{ svg, mapping, values }` 能接受吗? | 里程碑 A 的 main 槽位 | 时间 + 依赖清单 | 里程碑 A 用 `image` 占位 |
| 4.8 | **D5 时间**:第 1 周内能出 tbClient 0.1(C1 / C2 / C3 / ext)吗?之后每周大约几天? | 计划风险 3(关键路径) | 承诺 | 启用 legacy-adapter 预案,YY 自己包一版 |

## 6. 会后动作(YY)

1. 按结论改 ADR-001 / 003 / 004 状态为「已采纳」,ADR-002 直接采纳。
2. 契约:`ext.params` 定形、`DataSource.ext` 改必选、`.env.example` 的 `KZ_BASE` 改必配 → `pnpm gen:schema` → 测试 → `契约-v1.md` 标「已冻结」→ 双签。
3. 开发计划 T1.1 / T1.2 / T2.1 / T3.1 / T3.4 / T3.8 按 `T0.1-回填汇总与回应.md` §4 调整。
4. tbClient 交付时间未定 → 启动 legacy-adapter 预案;接线图暂缓 → `main` 槽位 `image` 占位。

---

会议时间:__________  到场:__________
结论记录人:__________
