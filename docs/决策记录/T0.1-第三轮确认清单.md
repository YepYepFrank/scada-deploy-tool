# T0.1 第三轮确认(只差 5 句话)

> 填写人:__________  日期:__________
> 发起:YY · 2026-09-06 · 接第二轮回填(2026-09-06 收到「反馈0907」版、庄艳芹版与项目接口-KEY清单,谢谢)
> **说明**:第二轮把四项决定都定下来了,这轮不是新问题,只是第二轮里空着没填的几格。每题一句话或一个链接就够,直接消息回复也行,不必填表。这轮的答案只影响 tbClient 里的参数映射和部署配置,不影响页面配置格式。
> docx 版:`T0.1-第三轮确认清单.docx`(发给同事的即此文件);解读依据见 `T0.1-第二轮回填汇总与回应.md`。
> **已回复(2026-09-06,高潮微信 + `Other/TB汇总业务-接口_20260818002.docx`)**:五题全答,解读见 `T0.1-第三轮回填汇总与回应.md`。

第二轮结论回顾(不用确认):

| 决定 | 我们的理解 |
|---|---|
| 告警 | 你们的体系是「资产上的告警模板 JSON + TB 规则链节点判定 + 写 TB 告警表」。一期工具不写设备模板、继续用自己的站点规则链;后续加「导出成你们的 JSON 格式」 |
| 命名 | 资产名 `REALTIME_* / HIST_* / STAT_REALTIME_*`,遥测 key camelCase 加业务前缀;`calc_` 不重叠。归档全量采遥测、不采服务端属性 |
| kz 接口 | 按设备 id / 站点 id / 量名 / 毫秒时间段查,TB token 鉴权,返回 `{ code, msg, data: { 量名: [{ ts, value, … }] } }`;曲线进 `series`,统计进 `meta` |
| tbClient | TbClient 留在庄艳芹的应用仓库;我们的包只放契约、预案与一致性套件(`@grid/tb-client/testing`) |

## 一、给后端 / 扩展服务同事(高潮)

**1. kz 通用查询接口的路径与粒度参数**(第二轮 3.1 / 3.4 空着)
- 接口路径:______  方法:[ ] GET [ ] POST
- 粒度参数的名字与取值(如 `interval = 1m / 5m / 1h / 1d / 1M`):______
- 设备 id / 站点 id / 量名 / 起止时间在请求里的字段名:______
- 有 curl / Postman 样例直接贴。默认:未答则通用查询暂不实现,长曲线先只用「收益趋势」。

**2. TB 遥测保留天数**(第二轮 3.5 空着)
- 保留 ______ 天;清理任务周期:______
- 用途:`ts-history` 只取该天数以内,超出改走 kz。默认 3 天。

**3. kz 在生产 / 现场的地址**(第二轮 3.6 空着)
- 反代前缀或地址:______(镜像用 `http://192.168.20.61:8099` / 同源 `/kz`)
- 默认:与 TB 同源、前缀 `/kzserver`。

**4. 告警 JSON 的存放位置与判定节点**(第二轮 1.3 追问)
- 模板 JSON 在哪个资产的哪个属性 key(如 `SITE_OVERVIEW` 的 `alarm_config`):______
- 告警设备清单的属性 key(如 `alarm_devices`):______
- 读它做判定的规则链节点名 / 所在规则链:______;镜像上 [ ] 有 [ ] 没有 [ ] 不清楚
- 默认:未答则一期只导出 JSON 文件,不写 TB 资产属性。

**5. 「资产改网关 + 遥测 key 加前缀」的时间表**(KEY 清单备注)
- 什么时候改:______;新前缀规则:______;旧资产 [ ] 直接删 [ ] 保留一段时间 [ ] 未定
- 页面按「类型 + 名称」引用资产,改名 / 删除后重新发布会报「解析不到」,校验能抓到,但要提前知道。默认:先按现有名称绑。

## 二、给庄艳芹(不用填,顺带告知)

TbClient 留在你的仓库。一致性套件已发布为 `@grid/tb-client/testing`(`FakeTb / FakeSocket / describeDataSourceConformance`,14 条,需 vitest ≥ 2),在你的仓库里跑就是验收。4.1 两种场景(HTTP 查不存在的 key、WS 订阅不存在的 key)都归一成 `null`,套件里有断言;kz `ext()` 曲线进 `series`、key:value 进 `meta`。

## 回复方式

消息回复、填好发回 YY、或提交到 `docs/决策记录/Other/` 均可。没答的题按「默认」执行,不再追问。

签字:后端同事 __________  庄艳芹 __________  日期 __________
