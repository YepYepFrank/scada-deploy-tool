# 联调环境(T0.4)

> 状态:**暂定 · 待补 id**(2026-09-03)。联调 / 验收站点定为 **润扬大桥项目**(镜像库中 `RY_*` 设备所属项目),后续可能调整;调整时改本文并在开发计划 T0.4 追加一行。
> 凭据一律不写在本文:tenant 与 CUSTOMER_USER 密码放 `dev/.env.local`(模板 `.env.example`)。
> 「实体清单」段由脚本生成:`node scripts/tb-discover.mjs "润扬"`,把输出粘贴到 §3;每次重新生成覆盖旧内容。

## 1. 目标 TB

| 项 | 值 |
|---|---|
| 地址 | `http://192.168.20.61:8080`(生产镜像,LXC 111,TB CE 4.3.1.3) |
| 扩展服务 | kzserver `http://192.168.20.61:8099`(仅 ADR-004 选 ext 路线时前端直连) |
| 开发代理 | `apps/deploy-tool/vite.config.js`:`/api` 与 `/tbm` → :8080,`/kz` → :8099 |
| 时区 | 全链 Asia/Shanghai(容器 / PVE / Postgres 已统一) |
| 数据规模 | 231 台设备、约 2,350 个遥测 key(2026-08-28 摸底);6 个网关代理子设备上报 |

## 2. 账号(只写名字)

| 用途 | 账号 | 备注 |
|---|---|---|
| 工具登录(tenant) | `tenant@thingsboard.org` | `.env.local` → `TB_USER / TB_PASSWORD` |
| Customer 视角预览 / 验收 | _待填:润扬大桥所属客户下的 CUSTOMER_USER_ | `.env.local` → `TB_CUSTOMER_USER / TB_CUSTOMER_PASSWORD`;由 §3 脚本输出的「客户账号」列选一个 |
| 旧大屏第二只读身份 | 现有报表账号(客户账号下的 CUSTOMER_USER) | 仅旧 `site.html` 三级取数用,T3.8 删除;`VITE_REPORT_USER / VITE_REPORT_PASSWORD` |

## 3. 实体清单(脚本生成,待运行)

_运行 `node scripts/tb-discover.mjs "润扬"` 后把输出粘贴到这里。需要至少包含:_

- 站点资产(名称 / type / id / 是否已有 `siteConfig`)
- 客户与其 CUSTOMER_USER 邮箱
- 代表性设备 ≥ 5 台,覆盖:网关、IED、电表、储能(BMS / PCS)、任一充电桩或 ATS;每台给 3 个以上正在上报的 key
- 若已有 `RT-*` / `HIS-*` 曲线资产(全站汇聚值),列出名称与 key

## 4. 里程碑 A 用到的实体(从 §3 挑选后填)

| 用途 | 实体 | key |
|---|---|---|
| number-card × 3(有功 / SOC / 电压) | | |
| gauge(SOC 或负载率) | | |
| status-light(通讯状态,`attr` 或 `COM`) | | |
| line 24h(全站汇聚,ASSET) | | `calc_` 或现有 RT-* 的 key |
| alarm-list(站点 Asset,types 待 ADR-001) | | |
| svg-diagram(同事接线图,main 槽位) | | 由同事给 mapping |

## 5. 验收前提核对(第 4 周开工前逐项打勾)

- [ ] §3 每个 id 用 `GET /api/asset/{id}` / `GET /api/device/{id}` 返回 200
- [ ] CUSTOMER_USER 登录成功,且能看到站点资产与 §4 全部设备
- [ ] 镜像可写:规则链 / 资产 / Relation / Customer 分配(D4 已问同事)
- [ ] 规则链已发布并运行,`calc_` key 有数据(否则页面绑了计算结果无数)
- [ ] 若站点换了,§3 / §4 重新生成,开发计划 T0.4 追加记录

## 6. 变更记录

| 日期 | 内容 |
|---|---|
| 2026-09-03 | 首版;站点暂定润扬大桥;实体清单待 `.env.local` 配好后由脚本生成 |
