# 联调环境(T0.4)

> 状态:**暂定 · 实体已摸底,待建站点资产与客户**(2026-09-03)。联调 / 验收站点定为 **润扬大桥 · 瓜州收费站储能**(镜像库 `RY_GZ_*` 设备),后续可能调整;调整时改本文并在开发计划 T0.4 追加一行。
> 凭据一律不写在本文:tenant 与 CUSTOMER_USER 密码放 `dev/.env.local`(模板 `.env.example`)。
> §3 由 `node scripts/tb-discover.mjs "润扬"` + 一次针对性探测生成(2026-09-03);换站点重跑覆盖。

## 1. 目标 TB

| 项 | 值 |
|---|---|
| 地址 | `http://192.168.20.61:8080`(生产镜像,LXC 111,TB CE 4.3.1.3) |
| 扩展服务 | kzserver `http://192.168.20.61:8099`(仅 ADR-004 选 ext 路线时前端直连) |
| 开发代理 | `apps/deploy-tool/vite.config.js`:`/api` 与 `/tbm` → :8080,`/kz` → :8099 |
| 时区 | 全链 Asia/Shanghai |
| 数据规模 | 231 台设备、约 2,350 个遥测 key;6 个网关代理子设备上报 |
| Device Profile(11 个,均无 alarmRules) | gateway · default · thermostat · JIZHAN_P1_DEVICELIST · 公司开发的充电桩 · IED · METER · ATS · OTA升级类 · 计算类 · 同步遥测类 |

## 2. 账号(只写名字)

| 用途 | 账号 | 状态 |
|---|---|---|
| 工具登录(tenant) | `tenant@thingsboard.org` | `.env.local` → `TB_USER / TB_PASSWORD` ✅ 已配 |
| Customer 视角预览 / 验收 | **待建**:客户 `润扬大桥`(建议名)下新建一个 CUSTOMER_USER | 见 §5 待办 ①;建好后填 `.env.local` → `TB_CUSTOMER_USER / TB_CUSTOMER_PASSWORD` |
| 现有客户账号(参考) | 客户 `客户账号` 下有 `jizhan@126.com` 与旧大屏用的报表账号(均 CUSTOMER_USER;报表账号名见 handoff,不在此重复) | 该客户只分配了 `BS_*` 基站设备与 39 个 RT/HIS/菜单/报表资产,**看不到润扬设备**,不能直接用作润扬的 Customer 视角 |

## 3. 实体清单(2026-09-03 摸底)

### 3.1 站点资产

**没有**。镜像里与润扬相关的资产为 0;现有 `tbsite` 资产只有 `xrs-mirror-test`(仙人山测试站,id `22e38540-a2c1-11f1-b6b5-f5e88fe257c3`)。
润扬的站点资产将由工具在首次发布时创建(名称建议 `ry-gz-ess`,type `tbsite`),`ScadaPage` 资产挂在它下面(契约 §5)。

### 3.2 客户

| 客户 | id | 用户 | 分配的润扬实体 |
|---|---|---|---|
| Public | `03fe0130-55ac-11f1-90ba-53cf2ab0fe96` | 无 | **`RY_GZ_ESS_PCS`**(疑为早期测试遗留,见 §5 待办 ③)+ 32 台仙人山 IED + 4 个测试资产 |
| 客户账号 | `36d54990-7b6f-11f1-8007-51b9f7714bbe` | jizhan@126.com、报表账号 | 无 |

### 3.3 设备(13 台:2 网关 + 11 子设备;子设备全部 Profile `default`,数据均为实时上报)

**网关**(`gateway` Profile,只上报 OTA 类 key,`Contains` 关系挂子设备)

| 网关 | id | Contains |
|---|---|---|
| `rydq_gz_ems`(润扬大桥瓜州收费站储能系统) | `57044800-6fb8-11f1-8007-51b9f7714bbe` | RY_GZ_ESS_PCS(关系表只登了这一台;其余 ESS 子设备实际也经此网关上报) |
| `rydq_gz_mgcc`(mgcc) | `f00973d0-6e01-11f1-8007-51b9f7714bbe` | RY_GZ_DLJKCOM |

**储能子系统**

| 设备 | 说明 | id | key 数 | 代表性 key |
|---|---|---|---|---|
| `RY_GZ_ESS` | 储能系统汇总(EMS 视角) | `f01bc350-6e01-11f1-8007-51b9f7714bbe` | 14 | `COM` `ESS_P` `ESS_Q` `BMS_SOC` `PCS_P` `PCS_Status` `PCS_Grid_State` `PCS_COM` `BMS_COM` `ESS_Ctrl_Mode` |
| `RY_GZ_ESS_PCS` | PCS | `5727ae80-6fb8-11f1-8007-51b9f7714bbe` | 205 | `AC_P_TOTAL` `AC_Q_TOTAL` `AC_PF` `GRID_F` `BUS_V_TOTAL` `BAT_V` `BAT_I` `DC_POWER` `OP_STATUS` `AMBIENT_T` `IGBT_T` `AC_CHG_DAY` `AC_DISCHG_DAY` + 约 150 个故障位 |
| `RY_GZ_ESS_BMS` | BMS | `580a1f40-6fb8-11f1-8007-51b9f7714bbe` | 453 | `SOC` `SOH` `BAT_V_TOTAL` `BAT_I_TOTAL` `CELL_V_MAX/MIN` `CELL_T_MAX/MIN` `V_AVG` `T_AVG` `TOTAL_CHG_CAP` `TOTAL_DISCHG_CAP` `RUN_STATUS` + 260 个单体 `nPnn_V`、80 个 `nPnn_T`、约 70 个告警位 |
| `RY_GZ_ESS_METER` | 储能电表 | `58ac2ba0-6fb8-11f1-8007-51b9f7714bbe` | 41 | `P` `Q` `S` `PF` `F` `Ua/Ub/Uc` `Uab/Ubc/Uac` `Ia/Ib/Ic` `EP` `EP-` `EP_PEAK/FLAT/VALLEY` |
| `RY_GZ_ESS_EXTM` | 储能外部电表 | `5776b8e0-6fb8-11f1-8007-51b9f7714bbe` | 41 | 同 METER |
| `RY_GZ_ESS_TMS` | 液冷机 | `586118e0-6fb8-11f1-8007-51b9f7714bbe` | 104 | `RUN_STATUS` `AMBIENT_T` `SUPPLY_T` `RETURN_T` `SUPPLY_P` `RETURN_P` `CABINET_T` + 告警位 |
| `RY_GZ_ESS_DEH` | 除湿机 | `58fc6e80-6fb8-11f1-8007-51b9f7714bbe` | 29 | `INT_T` `AMBIENT_T_VAL` `AMBIENT_H_VAL` `DEW_POINT_T` `HIGH_T_ALM` |
| `RY_GZ_ESS_SENS` | 安全检测传感器 | `57bd37c0-6fb8-11f1-8007-51b9f7714bbe` | 13 | `TEMPERATURE` `CO` `SYS_NORMAL` `SYS_L1..L4_ALM` `SMOKE_DET_ALM` |

**配电侧**

| 设备 | 说明 | id | key 数 | 代表性 key |
|---|---|---|---|---|
| `RY_GZ_DLJKCOM` | 电力监控管理机 | `69e81440-6eb1-11f1-8007-51b9f7714bbe` | 51 | `T1_UAB/UBC/UCA` `T2_*` `401_P` `401_Q` `402_P` `402_Q` `T1_CB` `T2_CB` `401_CB` `402_CB` `LP3_n_CB` `LP4_n_CB` `LP7_n_CB` |
| `RY_GZ_LSA1420` | 并网柜测控 | `f0a2cda0-6e01-11f1-8007-51b9f7714bbe` | 53 | `P_Total` `Q_Total` `S_Total` `PF_Total` `Freq` `Ua/Ub/Uc` `Ia/Ib/Ic` `EP` `Reverse_Active_Energy` `P_Demand_Real` `QF1..QF5` |
| `RY_GZ_BAOHU` | 防逆流装置 | `f06660e0-6e01-11f1-8007-51b9f7714bbe` | 25 | `P` `Q` `Uab/Ubc/Uca` `Ia/Ib/Ic` `cos` `Device_Alarm` `Reverse_Power_Stage1/2` `OC_Stage1/2/3` |

### 3.4 可复用的全站汇聚资产(全库共用,不分站点;`客户账号` 可见)

`REALTIME_*`(type `RT-…`,key 如 `tsGridExchangePower` `tsLoadPowerForecast`)、`HIST_*`(type `HIS-…`,key 如 `histConsumptionTotal` `histStorageEnergyCharge`)共 26 个,由 kz 归档写入。它们是全站级、非润扬专属,一期页面若要用需确认口径;里程碑 A 不依赖。

## 4. 里程碑 A 用到的实体(建议,T2.7 手写 JSON 时照此填)

| 组件 | 实体 | key / 绑定 | 备注 |
|---|---|---|---|
| number-card:储能有功 | `RY_GZ_ESS_PCS` | `ts` `AC_P_TOTAL`(kW) | 正放电负充电,按现场约定 |
| number-card:SOC | `RY_GZ_ESS_BMS` | `ts` `SOC`(%) | |
| number-card:并网点有功 | `RY_GZ_LSA1420` | `ts` `P_Total`(kW) | |
| gauge:SOC | `RY_GZ_ESS_BMS` | `ts` `SOC` min 0 max 100 | 与上重复无妨,验证两种组件 |
| status-light:PCS 通讯 | `RY_GZ_ESS` | `ts` `PCS_COM`(或 `COM`) | onValue 按现场:1 = 正常 |
| line 24h:储能功率 | `RY_GZ_ESS_PCS` | `ts-history` `AC_P_TOTAL` window `24h` agg `AVG` | 站点资产建好后改绑 `calc_` 汇聚 key |
| dual-axis:电表 P / Q | `RY_GZ_ESS_METER` | `ts-history` `P` + `Q` window `2h` | |
| alarm-list | `RY_GZ_ESS`(暂)→ 站点资产(建好后) | `alarm`,types 待 ADR-001 | 当前库内**无任何 alarmRules 与站点告警链**,需先用工具发布一条(如 BMS `SOC` < 10 或 PCS `COM` = 0) |
| table:电池极值 | `RY_GZ_ESS_BMS` | `ts` `CELL_V_MAX` `CELL_V_MIN` `CELL_T_MAX` `CELL_T_MIN` | |
| svg-diagram(main) | 同事接线图 | mapping 由同事给 | 未就绪则 `image` 占位 |

## 5. 待办(第 1 周内,负责人 YY)

- [ ] **① 建客户与账号**:TB 里新建 Customer `润扬大桥`,下建一个 CUSTOMER_USER(邮箱自定),把 §3.3 的 13 台设备分配给它;密码进 `.env.local`。这是 Customer 视角预览(T3.6)与 D4 验收的前提。_也可让同事按其生产习惯建,但要在 D5 前有。_
- [ ] **② 建站点资产**:用向导对润扬走一遍「连接 → 认领 13 台设备 → 发布」,产生 `tbsite` 站点资产与首批 `calc_` 汇聚(如 ESS 有功 + 并网点有功);同时发布 1 条站点级告警供 alarm-list 验证。
- [ ] **③ 处理 `RY_GZ_ESS_PCS` 的 Public 分配**:一台真实设备挂在 Public 客户上意味着匿名可读;确认是早期测试遗留后取消分配(T3.8 删 Public 路径时一并清理,或现在手工取消)。
- [ ] **④ Profile 提醒**:润扬 11 台子设备全在 `default` Profile,与 `thermostat` 等杂项同槛。A1 若把「通讯中断」写进 Profile,会作用到库里所有 `default` 设备——要么先给润扬建专属 Profile(`ESS_PCS / ESS_BMS / …`),要么此站点的告警全部走站点规则链。已记入 ADR-001 与 T0.1 清单 A1。

## 6. 验收前提核对(第 4 周开工前逐项打勾)

- [ ] §3 每个 id 用 `GET /api/device/{id}` 返回 200(2026-09-03 摸底时全部在线,最新遥测 ≤ 1 分钟)
- [ ] 待办 ① 完成,CUSTOMER_USER 登录成功且能看到 13 台设备与站点资产
- [ ] 待办 ② 完成,`calc_` key 有数据
- [ ] 镜像可写:规则链 / 资产 / Relation / Customer 分配(D4 已问同事;tenant 账号实测可读全部,写权限随 ② 验证)
- [ ] 若站点换了,§3 / §4 重新生成,开发计划 T0.4 追加记录

## 7. 变更记录

| 日期 | 内容 |
|---|---|
| 2026-09-03 | 首版;站点暂定润扬大桥瓜州收费站储能;摸底 13 台设备 / 2 客户 / 11 Profile;发现无站点资产、无专属客户、PCS 挂 Public、子设备全 `default` Profile |
