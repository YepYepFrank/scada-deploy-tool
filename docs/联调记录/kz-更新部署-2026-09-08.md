# kz(tb-api-admin)更新部署与鉴权复测 · 2026-09-08

> 联调环境待办 ⑤ 的闭环记录。高潮 09-07 晚确认 `tskv/**` 不校验 token 是配置错误,交来新包 `resource/kzserver/tb-api-admin-updated.jar`(仓库外);09-08 09:50(CST)部署到镜像 CT 111,复测通过。

## 1. 部署

| 项 | 值 |
|---|---|
| 目标 | PVE `192.168.10.2` → LXC 111(`tb-mirror`,TB 镜像同机),systemd `tb-api-admin`,`/opt/kzserver/tb-api-admin.jar`,JVM `-Xmx768m`,Java 17.0.20.1 |
| 旧包 | md5 `b0a7b5f9da0f5c8bb25156689126affc`(= `resource/kzserver/tb-api-admin.jar`,08-31 部署) |
| 新包 | md5 `fb8e502a7e13933bc455853b296432bc`(`tb-api-admin-updated.jar`,92 791 367 B) |
| 备份 | 容器内 `/opt/kzserver/tb-api-admin.jar.bak-20260907`(旧包原样) |
| 步骤 | SFTP 上传到 PVE `/root` → `pct push 111` → 容器内 `cp -p` 备份、`mv` 覆盖、`systemctl restart tb-api-admin` → 6 秒启动完成,8099 监听 |
| 配置 | `application-gz.yml` 未动(数据源仍指本机 TB 库、Redis db5) |
| 回退 | `pct exec 111 -- bash -c 'cd /opt/kzserver && cp -p tb-api-admin.jar.bak-20260907 tb-api-admin.jar && systemctl restart tb-api-admin'` |

顺带看到的:重启前日志里有 `slow sql 27–35 s. INSERT INTO biz_ts_kv_minute`(旧进程,归档写入偶发很慢),与本次无关,先记一笔;三处时钟一致(PVE / CT 111 CST,本机 UTC+0 差 8 小时)。

## 2. 鉴权复测(`kz-auth-probe2` 直打接口 + `pnpm -F @grid/tb-client test:live`)

| 请求 | `tskv/minute/…`(更新前) | `tskv/minute/…`(更新后) | `biz/tb/menu` |
|---|---|---|---|
| 有效租户 token | 200 数据 | `code 200` 59 点 | `code 500 类型字段不能为空`(参数问题,鉴权已过) |
| 有效客户 token,已分配设备 | 200 数据 | `code 200` 59 点 | — |
| 无 / 空 header | `code 500 token不能为空` | `code 500 token不能为空` | 同 |
| `Bearer nope` | **200 数据** | `code 401 token失效` | `code 401 token失效` |
| 篡改签名 / 篡改 payload 的 JWT | **200 数据** | `code 401 token失效` | — |
| 过期 JWT(原签名,exp=2001) | **200 数据** | `code 401 token失效`(live 用例) | — |
| token 不带 `Bearer` | `token不能为空` | `code 401 token失效` | — |

`test:live` 12 条:`kz-ext` 7/7、`kz-auth` 4/4 + 1 todo,全绿。`kz-auth.live.ts` 的 `KZ_AUTH_FIXED` 门控已去掉,随 `test:live` 常跑——现场 kz 若装了旧版本,这组会红。

客户视角大屏(`xrs-viewer` 登录单文件 `site.html`,「运营总览」):两条 kz 30 天曲线在客户 token 下照常有数,顶栏无「不可见」、组件无「不可用」(截图 `img/归属迁移-客户视角-2026-09-07.jpg` 为更新后重截)。

## 3. 仍未做的一步:按实体归属过滤(交高潮,低优先)

token 校验现在与 `biz/**` 一致,但 **`tskv/**` 不看 token 的身份能否访问该实体**(`kz-auth-probe3`,更新后):

| 设备(客户 `xrs-viewer` 未分配) | 客户 token 调 kz | TB 侧 `GET /api/device/{id}` 同 token |
|---|---|---|
| `Test Device A1`(租户私有) | `code 200`,46 点 | 403 |
| `Test Device A2`(租户私有) | `code 200`,23 点 | 403 |
| `RY_GZ_ESS_PCS`(润扬,Public) | `code 200`,69 点 | 403 |

后果:任何**能登录**的客户账号都能读全库归档,跨客户隔离在 kz 侧不成立(相比更新前「任何人」已收窄到「任何合法用户」)。建议做法同 09-07 记录:kz 对 `tskv` 的 `{entityType}/{entityId}` 用同一 token 查一次 TB `/api/{type}/{id}`,非 2xx 就拒;或按 token 里的 `customerId` 过滤。已在 `kz-auth.live.ts` 记 `it.todo`,联调环境待办 ⑤ 拆出 ⑤b。生产部署前如未修,kz 仍只走同机反代、不对客户网络开放,与一期客户账号都是同一项目客户的现状下风险可接受。
